***REMOVED*** Pre-commit hook configuration
***REMOVED*** Install: pip install pre-commit && pre-commit install
***REMOVED*** See: https://pre-commit.com

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        name: Detect secrets with Gitleaks
        description: Prevent committing secrets and credentials
        args: ["--config=.gitleaks.toml"]
