eureka:
  instance:
    hostname: eureka
    metadata-map:
      type: data
      label: Superset Proxy
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka

spring:
  application:
    name: superset-proxy
server:
  port: 8080

